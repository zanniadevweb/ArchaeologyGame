<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.test_libgdxintoandroid" >

      <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
-
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


          <activity
              android:name=".LayerActivity"
              android:label="@string/app_name">
          </activity>

          <activity
              android:name=".LayerActivity2"
              android:label="@string/app_name">
          </activity>

          <activity
              android:name=".ProfilActivity"
              android:label="@string/app_name">
          </activity>

          <activity
              android:name=".Findings"
              android:label="@string/app_name">
          </activity>

          <activity
              android:name=".Personnage"
              android:label="@string/app_name">
          </activity>

          <activity
              android:name=".ObserveLayerActivity"
              android:label="@string/app_name">
          </activity>

          <activity
              android:name=".RemoveFirstHumanLayerActivity"
              android:label="@string/app_name">
          </activity>

      </application>
  </manifest>